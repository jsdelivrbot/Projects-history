// import { fromJS } from 'immutable';
// import { makeSelectLogoutDomain } from '../selectors';

// const selector = makeSelectLogoutDomain();

describe('makeSelectLogoutDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
