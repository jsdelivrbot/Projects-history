// import { fromJS } from 'immutable';
// import { makeSelectErrorContainerDomain } from '../selectors';

// const selector = makeSelectErrorContainerDomain();

describe('makeSelectErrorContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
