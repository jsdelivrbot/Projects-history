// import { fromJS } from 'immutable';
// import { makeSelectReportDisplayDomain } from '../selectors';

// const selector = makeSelectReportDisplayDomain();

describe('makeSelectReportDisplayDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
