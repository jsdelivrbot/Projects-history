// import { fromJS } from 'immutable';
// import { makeSelectRedirectDomain } from '../selectors';

// const selector = makeSelectRedirectDomain();

describe('makeSelectRedirectDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
