// import { fromJS } from 'immutable';
// import { makeSelectAuthRequiredDomain } from '../selectors';

// const selector = makeSelectAuthRequiredDomain();

describe('makeSelectAuthRequiredDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
