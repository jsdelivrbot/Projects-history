// import { fromJS } from 'immutable';
// import { makeSelectMedicationListDomain } from '../selectors';

// const selector = makeSelectMedicationListDomain();

describe('makeSelectMedicationListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
