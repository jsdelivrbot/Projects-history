// import { fromJS } from 'immutable';
// import { makeSelectAddNewMedicationDomain } from '../selectors';

// const selector = makeSelectAddNewMedicationDomain();

describe('makeSelectAddNewMedicationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
