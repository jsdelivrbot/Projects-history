/*
 * CheckLog Messages
 *
 * This contains all the text for the CheckLog component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.CheckLog.header',
    defaultMessage: 'Medication check log',
  },
});
