// import { fromJS } from 'immutable';
// import { makeSelectCheckLogDomain } from '../selectors';

// const selector = makeSelectCheckLogDomain();

describe('makeSelectCheckLogDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
