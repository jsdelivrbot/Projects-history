// import { fromJS } from 'immutable';
// import { makeSelectAdministeredMedicationDomain } from '../selectors';

// const selector = makeSelectAdministeredMedicationDomain();

describe('makeSelectAdministeredMedicationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
