// import { fromJS } from 'immutable';
// import { makeSelectAllergiesDomain } from '../selectors';

// const selector = makeSelectAllergiesDomain();

describe('makeSelectAllergiesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
