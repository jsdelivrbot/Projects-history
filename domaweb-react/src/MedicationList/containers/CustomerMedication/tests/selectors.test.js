// import { fromJS } from 'immutable';
// import { makeSelectCurrentMedicationDomain } from '../selectors';

// const selector = makeSelectCurrentMedicationDomain();

describe('makeSelectCurrentMedicationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
