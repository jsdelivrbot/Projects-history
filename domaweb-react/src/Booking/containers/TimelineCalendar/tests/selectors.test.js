// import { fromJS } from 'immutable';
// import { makeSelectBookingDomain } from '../selectors';

// const selector = makeSelectBookingDomain();

describe('makeSelectBookingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
